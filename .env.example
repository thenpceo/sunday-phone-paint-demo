# Optional locally: use an HTTPS tunnel origin so a phone can open the camera route.
PHONE_PAINT_PUBLIC_ORIGIN=https://your-secure-origin.example

# Required for reliable serverless cross-device sessions.
# Vercel's Upstash Redis integration supplies these automatically.
KV_REST_API_URL=
KV_REST_API_TOKEN=
